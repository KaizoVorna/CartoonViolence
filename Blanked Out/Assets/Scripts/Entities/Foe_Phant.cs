using UnityEngine;

public class Foe_Phant : MonoBehaviour
{
    //This is the behaviour script for Mr. Phant after he becomes hostile.
    //To Do:
    //-A hitbox that allows him to walk over 1-tile-gaps.
    //-Create an angry state that has him chase the player, triggered by vision or hearing.
    //-Create an ability to reach to a higher level or reach through 1-tile gaps, but not jump.
    //-Create an ability to smash through weak walls.
    //-Create an animation for when he catches the player and kills them.

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
