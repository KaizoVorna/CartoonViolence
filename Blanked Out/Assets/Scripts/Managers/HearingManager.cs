using UnityEngine;

public class HearingManager : MonoBehaviour
{
    //This is the manager for enemies' hearing radius.
    //To Do:
    //-Create 2 hearing radiuses: A larger one for loud noises, smaller for quiet noises.
    //-Create a trigger to toggle enemies' alert state if certain object or entity behaviour is detected. (e.g. Player runs.)

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
