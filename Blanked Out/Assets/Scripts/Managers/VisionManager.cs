using UnityEngine;

public class VisionManager : MonoBehaviour
{
    //This manager handles enemies' vision behaviour.
    //To Do:
    //-Create trigger colliders for vision cones.
    //-Send a signal for enemies to enter aggressive state if player enters the trigger without invisibility.

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
