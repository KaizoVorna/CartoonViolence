using UnityEngine;

public class Obj_Campfire : MonoBehaviour
{
    //This is the script for the spawned campfire.
    //To Do:
    //-Generates a passive trigger collider that prevents enemies from approaching and triggers the panic script for some enemies.
    //-Make it also generate light.
    //-Should we have rope/web burning yet?

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
