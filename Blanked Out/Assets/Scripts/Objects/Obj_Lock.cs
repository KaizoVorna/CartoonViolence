using UnityEngine;

public class Obj_Lock : MonoBehaviour
{
    //This is the behaviour for the lock.
    //To Do:
    //-Make it so that it's destroyed by coming into contact with the Key object.
    //-Make it disable the interaction of the thing it's attached to (door, switch)

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
