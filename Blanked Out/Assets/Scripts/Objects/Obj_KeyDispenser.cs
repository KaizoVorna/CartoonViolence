using UnityEngine;

public class Obj_KeyDispenser : MonoBehaviour
{
    //This is the object behaviour for the Key Dispenser.
    //To Do:
    //-Make it spawn a key when struck with the drawing pad, but only if there's no key object already in play.

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
