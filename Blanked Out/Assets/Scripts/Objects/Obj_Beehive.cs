using UnityEngine;

public class Obj_Beehive : MonoBehaviour
{
    //This is the behaviour script for the Beehive.<Secret Only, Low Priority>
    //To Do:
    //-Make it fall when it collides with an object or a projectile.
    //-When it hits the ground, make it spawn a bee cloud object. Destroy the beehive after.

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
